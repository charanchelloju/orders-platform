apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orders-ingress
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/charanchelloju/orders-platform.git
    targetRevision: main
    path: helm/orders-ingress
    helm:
      releaseName: orders-ingress
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: orders-platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
